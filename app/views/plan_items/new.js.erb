<%- day_selector = "#day_#{@day.id}" %>
<%- add_link = "#{day_selector} .add-plan-item" %>
$('<%= add_link %>').before('<%= escape_javascript render('form') %>');
$('<%= add_link %>').hide();

// Cancel link
$('<%= day_selector %> #cancel-add-plan-item').click(function(event) {
  $(this).closest('form').remove();
  $('<%= add_link %>').show();
  event.preventDefault();
});
